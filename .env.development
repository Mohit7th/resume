# Loaded by Vite in dev (serve) mode. Provides VITE_SITE_URL so the
# %VITE_SITE_URL% placeholders in index.html resolve during local development
# (production uses .env.production / the deploy workflow).
VITE_SITE_URL=https://mohit7th.github.io/resume
